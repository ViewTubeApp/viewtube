export interface WebVTTConfig {
  interval: number;
  numColumns: number;
  width: number;
  height: number;
}

export const WEBVTT_CONFIG: WebVTTConfig = {
  interval: 1,
  numColumns: 5,
  width: 160,
  height: 90,
};
