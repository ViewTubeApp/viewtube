# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# General
UPLOADS_VOLUME=""
NEXT_PUBLIC_BRAND=""
NEXT_PUBLIC_URL=""
NEXT_PUBLIC_CDN_URL=""

# Auth (Authentik)
AUTH_URL=""                              # Sign in URL (e.g., https://yourdomain/api/auth)
AUTH_TRUST_HOST=""                       # See https://authjs.dev/getting-started/deployment#auth_trust_host
AUTHENTIK_SECRET_KEY=""                  # Secret key for Authentik encryption
AUTHENTIK_AUTH_ISSUER=""                 # OAuth issuer URL (e.g., https://auth.yourdomain/application/o/viewtube/)
AUTHENTIK_AUTH_CLIENT_ID=""              # OAuth client ID from Authentik
AUTHENTIK_AUTH_CLIENT_SECRET=""          # OAuth client secret from Authentik

# Database
POSTGRES_HOST=""                         # PostgreSQL host (e.g., db for Docker Swarm)
POSTGRES_DB=""                           # Web application database name
POSTGRES_PORT=""                         # Database port (default: 5432)
POSTGRES_USER=""                         # Database user
POSTGRES_PASSWORD=""                     # Database password

# RabbitMQ
RABBITMQ_HOST=""                        # RabbitMQ host (e.g., rabbitmq for Docker Swarm)
RABBITMQ_PORT=""                        # RabbitMQ port (default: 5672)
RABBITMQ_USER=""                        # RabbitMQ user
RABBITMQ_PASSWORD=""                    # RabbitMQ password

# pgAdmin
PGADMIN_EMAIL=""                        # Admin email for pgAdmin login
PGADMIN_PASSWORD=""                     # Admin password for pgAdmin login